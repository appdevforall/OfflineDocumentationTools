# The Dokka *worker* does the heavy lifting and is sized by dokkaGeneratorIsolation in
# build.gradle.kts, not from here. The daemon itself only needs enough to run the build.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g
