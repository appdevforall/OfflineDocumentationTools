# Everything the scripts in this directory import, as one installable list:
#
#     pip install -r requirements.txt
#
# They live here rather than in the repo-root requirements.txt because they are this
# directory's alone - cairosvg in particular wants a native libcairo at import time, which
# every other consumer of the root file would then be on the hook for. The root file is still
# pulled in below, since these scripts also use its brotli/Pillow.
-r ../../../requirements.txt

# md_to_json imports markdown_it; populate_db and insert_optimized_media import md_to_json.
markdown-it-py>=3.0
# optimize_media (and insert_optimized_media through it) imports scour, and imports cairosvg
# to rasterize an SVG still over --svg-rasterize-threshold after optimization.
scour>=0.38
cairosvg>=2.7
